"use client"; import { useLang } from "@/context/LanguageContext"; import { DIRECT_PRICING, PRICING_CATEGORIES, LABOR_TERMS } from "@/lib/pricing"; import PageShell from "@/components/marketing/PageShell"; import MarketingHero from "@/components/marketing/MarketingHero"; import Container from "@/components/ui/Container"; import Button from "@/components/ui/Button";
const names={websites:{en:'Websites',es:'Sitios Web'},ai:{en:'AI Tools & Automation',es:'Herramientas y Automatización con IA'},crm:{en:'CRM Systems',es:'Sistemas CRM'},branding:{en:'Branding',es:'Marca'},social:{en:'Social Media',es:'Redes Sociales'}} as const;
export default function PricingPage(){const{lang}=useLang();return <PageShell><MarketingHero eyebrow={lang==='en'?'Transparent starting prices':'Precios iniciales transparentes'} title={lang==='en'?'Choose a clear starting point for your project':'Elija un punto de partida claro para su proyecto'} description={lang==='en'?'Packages establish a practical baseline. Final scope depends on pages, integrations, content, complexity, migration, and third-party requirements.':'Los paquetes establecen una base práctica. El alcance final depende de páginas, integraciones, contenido, complejidad, migración y requisitos de terceros.'} primaryHref="/checkout" primaryLabel={lang==='en'?'Start an Order':'Iniciar una orden'} secondaryHref="/contact" secondaryLabel={lang==='en'?'Request a Custom Quote':'Solicitar cotización'}/><section className="py-20"><Container className="space-y-16">{PRICING_CATEGORIES.map(cat=><div key={cat}><h2 className="text-3xl font-semibold">{names[cat][lang]}</h2><div className="mt-7 grid gap-5 md:grid-cols-3">{DIRECT_PRICING.filter(x=>x.category===cat).map(t=><article key={t.id} className="rounded-2xl border border-[rgba(14,165,233,.15)] bg-vx-bg2 p-7"><h3 className="text-xl font-semibold">{t.nameKey.replace(/([A-Z])/g,' $1')}</h3><p className="mt-5 font-mono text-3xl text-vx-cyan">{t.price}</p><p className="mt-2 text-sm text-vx-muted">{t.unitKey==='perMonth'?(lang==='en'?'per month':'por mes'):(lang==='en'?'one-time starting price':'precio inicial único')}</p>{t.recurring?<p className="mt-3 text-sm text-vx-silver">+ {t.recurring} / {lang==='en'?'month':'mes'}</p>:null}</article>)}</div></div>)}<div className="rounded-2xl border border-[rgba(34,211,238,.2)] bg-vx-bg2 p-7"><h2 className="text-2xl font-semibold">{lang==='en'?'Important pricing terms':'Términos importantes'}</h2><p className="mt-4 text-vx-muted">{lang==='en'?`Additional labor is ${LABOR_TERMS.hourly} per hour and rush work is ${LABOR_TERMS.rush} per hour when accepted. Third-party hosting, domains, software, messaging, advertising, and platform fees are separate.`:`La labor adicional cuesta ${LABOR_TERMS.hourly} por hora y el trabajo urgente ${LABOR_TERMS.rush} por hora cuando sea aceptado. Hosting, dominios, software, mensajería, publicidad y plataformas de terceros son separados.`}</p><div className="mt-6"><Button href="/contact">{lang==='en'?'Clarify Your Scope':'Aclarar su alcance'}</Button></div></div></Container></section></PageShell>}
